# > < <= >= != ==
# if is used to ask questions
# they are boolean expressions 

# as per the questions the values are displayed 
# as per the questions the values are displayed
num = int(input("Enter a Number: "))
if num > 0:
    print("Positive")
else:
    print("Negative")

# Ternary Operator
print("Positive") if num > 0 else print("Negative")
